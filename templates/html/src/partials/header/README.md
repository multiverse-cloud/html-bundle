# Header Partial

Runtime file: `../../../../common-header.js`

TailAdmin keeps shared header UI in partials/components so every page gets the same search, sidebar toggle, notification, dark-mode, and profile behavior.
